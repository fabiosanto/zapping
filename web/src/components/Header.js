import * as React from "react"
import { Link } from "gatsby"

const Header = (data) => {
    return (
        <div className="p-3 h-10 text-white bg-white bg-opacity-5 md:flex">
        <div className="m-auto flex">Tivy Header</div>
     </div>)
}

export default Header